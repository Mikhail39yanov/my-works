export enum EApiTimers {
  apiTimers = 'api/timers',
}

export enum EActionCLIApiClient {
  signup = 'signup',
  login = 'login',
  logout = 'logout',
  status = '?isActive=',
  start = '',
  stop = 'stop',
}
